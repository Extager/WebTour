name: Test-React-App
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Uses Checkout
        uses: actions/checkout@v4
      - name: Install dependencies
        run: npm ci
      